pipeline{
    agent any
    stages{
        stage("Git Checkout"){
            steps{
                git branch: 'main', url: 'https://github.com/siswarnava-sss/ecommerce2'
            }
        }
        stage{
            steps{
                sh 'mvn test'
            }
        }
        stage{
            steps{
                sh 'mvn verify -DskipUnitTest'
            }
        }
    }
}
